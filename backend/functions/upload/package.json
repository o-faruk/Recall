{
  "name": "recall-upload-lambda",
  "version": "1.0.0",
  "description": "Recall upload Lambda — API Gateway POST -> S3 + DynamoDB",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "zip": "rm -f function.zip && zip -r function.zip index.js node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
