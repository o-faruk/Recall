{
  "name": "recall-delete-lambda",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "description": "Recall delete Lambda — DELETE /documents/{id} removes DynamoDB + S3 + Pinecone",
  "scripts": {
    "zip": "rm -f function.zip && zip -r function.zip index.js package.json node_modules/"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@pinecone-database/pinecone": "^3.0.0"
  }
}
